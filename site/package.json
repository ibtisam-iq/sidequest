{
  "name": "sidequest-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "predev": "node ../scripts/gen-zod-schemas.mjs",
    "dev": "astro dev",
    "prebuild": "node ../scripts/gen-zod-schemas.mjs",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.4",
    "js-yaml": "^5.3.0",
    "pagefind": "^1.5.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "json-schema-to-zod": "^2.8.1",
    "typescript": "^6.0.3"
  }
}
